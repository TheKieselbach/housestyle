"""housestyle — measure your own writing voice locally.

This directory is the package when installed with pip, and a plain script
directory in a git checkout or Claude Code plugin. Both layouts work; see
config.py for how the paths are resolved.
"""
__version__ = "0.1.0"
